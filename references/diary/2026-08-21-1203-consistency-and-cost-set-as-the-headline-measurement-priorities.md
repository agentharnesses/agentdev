Closes the loop this session opened at `2026-08-21-0811` ("doubting end_accuracy as the signal to chase"). User's stated general rule: "knowing if an answer is right or wrong is hard, but knowing if it's consistent in nature and how expensive it is to arrive at that answer seems worthwhile to measure and practically measurable." Concretely: going forward, the three things actually worth tracking are **the LLM-judge consistency eval** (`run_consistency_elo`), **the document-visit (file-touch) consistency eval** (`file_consistency.py`), and **cost/tokens/time** (`metrics.py`'s `tokens`/`input_tokens`/`output_tokens`/`wall_clock_seconds`, plus priming's own separately-tracked cost).

This is the actual conclusion of a day-long arc, not a new idea: `end_accuracy` was doubted this morning on the grounds that every real check failure so far turned out to be a check-design problem, not a genuine answer-quality gap, while cost/time metrics never needed that kind of rescue. Quality Elo was built as one candidate replacement, but its own validation pattern told the same story — quality's competitor-format bug, the priming-vs-real-work token confound, and the general difficulty of the LLM judge's "which is better" question (compared to consistency's cleaner "do these agree") all pointed the same direction. Consistency, by contrast, had two independent alternative signals validated against each other today — embedding-based (rejected, -0.17 correlation) and file-touch-based (kept, +0.39 correlation) — and both experiments were cheap and conclusive specifically *because* "do these repeats agree" is a much better-posed question than "is this answer good."

Not a decision to delete anything — `run_quality_elo`, `grading.py`'s checks-based `end_accuracy`, and `embeddings.py` all stay in the codebase as real, tested, working code. This is a decision about where attention and default reporting go, not what gets torn out: for now, a run's story is best told by consistency (two independent measurements of it) and cost (tokens split by input/output, wall-clock, priming tracked separately) — not by trying to adjudicate correctness at all.

Open question, not yet acted on: does this change what `reporting-results/SKILL.md`'s default chat table shows, or whether `elo`'s default behavior should skip computing quality Elo to save the judge-call cost when it's not the thing being reported? Left for whenever that's picked up rather than assumed here.
